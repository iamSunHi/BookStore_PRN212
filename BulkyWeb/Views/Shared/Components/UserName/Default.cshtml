@model string

<li class="nav-item dropdown-center">
    <a class="nav-link dropdown-toggle py-3" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        @Model
    </a>
    <ul class="dropdown-menu shadow" style="left: unset; right: 0;">
        <li>
            <a id="manage" class="dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Account</a>
        </li>
        <li><hr class="dropdown-divider"></li>
        <li>
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="dropdown-item">Logout</button>
            </form>
        </li>
    </ul>
</li>